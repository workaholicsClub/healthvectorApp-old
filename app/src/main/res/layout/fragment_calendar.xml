<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/calendar_header_margin_horizontal"
        android:layout_marginRight="@dimen/calendar_header_margin_horizontal"
        android:gravity="end" />

    <include layout="@layout/calendar_header" />

    <GridView
        android:id="@+id/gridViewCalendar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/calendar_grid_view_height"
        android:layout_marginLeft="@dimen/calendar_grid_view_margin_horizontal"
        android:layout_marginRight="@dimen/calendar_grid_view_margin_horizontal"
        android:horizontalSpacing="@dimen/calendar_horizontal_spacing"
        android:listSelector="@android:color/transparent"
        android:numColumns="7"
        android:stretchMode="columnWidth"
        android:verticalSpacing="@dimen/calendar_vertical_spacing" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="@dimen/calendar_header_margin_vertical"
        android:layout_weight="1"
        android:background="@color/background">

        <android.support.v7.widget.RecyclerView
            android:id="@+id/events"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/calendar_header_margin_vertical" />
    </LinearLayout>
</LinearLayout>
