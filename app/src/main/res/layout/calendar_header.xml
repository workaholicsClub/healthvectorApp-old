<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginLeft="@dimen/calendar_margin_horizontal"
    android:layout_marginRight="@dimen/calendar_margin_horizontal"
    android:gravity="center"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/calendarTitle"
        android:layout_width="0dp"
        android:layout_height="@dimen/calendar_title_height"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:textAppearance="@style/TitleTextAppearance" />

    <ImageView
        android:id="@+id/left"
        android:layout_width="@dimen/calendar_title_height"
        android:layout_height="@dimen/calendar_title_height"
        android:background="@drawable/background_clickable"
        android:contentDescription="@string/empty"
        android:scaleType="center"
        android:src="@drawable/arrow_left" />

    <ImageView
        android:id="@+id/right"
        android:layout_width="@dimen/calendar_title_height"
        android:layout_height="@dimen/calendar_title_height"
        android:background="@drawable/background_clickable"
        android:contentDescription="@string/empty"
        android:scaleType="center"
        android:src="@drawable/arrow_right" />
</LinearLayout>
